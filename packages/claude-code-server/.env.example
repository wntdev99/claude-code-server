DATABASE_URL="file:../core/prisma/dev.db"

# Required for encrypting sensitive settings (API keys, tokens)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=
