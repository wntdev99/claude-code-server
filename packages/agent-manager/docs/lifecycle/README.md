# 에이전트 생명주기 관리

에이전트 프로세스의 생명주기 관리에 관한 문서 모음입니다.

## 📄 문서 목록

### `creation.md`
**언제 읽나요?** 새 에이전트를 생성해야 할 때

**내용**:
- 에이전트 프로세스 spawn
- 작업 디렉토리 준비
- 환경 변수 주입
- 초기 프롬프트 구성
- CLAUDE.md 로딩

### `execution.md`
**언제 읽나요?** 에이전트를 제어해야 할 때

**내용**:
- 에이전트 시작 (start)
- 일시 중지 (pause) - SIGTSTP
- 재개 (resume) - SIGCONT
- 취소 (cancel)
- stdin/stdout/stderr 관리

### `termination.md`
**언제 읽나요?** 에이전트를 종료하고 정리해야 할 때

**내용**:
- 정상 종료 (graceful shutdown)
- 강제 종료 (SIGKILL)
- 리소스 정리
- 로그 저장
- 프로세스 상태 정리

## 🎯 생명주기 흐름

```
idle (생성 전)
    ↓
[create] → Agent 생성
    ↓
idle (대기)
    ↓
[start] → 실행 시작
    ↓
running (실행 중)
    ├─[pause] → paused (일시 중지)
    │    ↓
    │  [resume] → running
    │
    ├─[dependency/question 감지] → waiting_* (대기)
    │    ↓
    │  [provide/answer] → running
    │
    ├─[phase complete] → waiting_review (리뷰 대기)
    │    ↓
    │  [approve] → running (다음 Phase)
    │
    ├─[cancel] → terminating
    │
    └─[complete] → completed
         ↓
      [terminate] → 정리 및 종료
```

## 🔍 빠른 참조

### 에이전트 생성
→ `creation.md` → "프로세스 Spawn" 섹션

### 에이전트 일시중지
→ `execution.md` → "일시 중지" 섹션

### 에이전트 재개
→ `execution.md` → "재개" 섹션

### 에이전트 종료
→ `termination.md` → "정상 종료" 섹션

## 📊 문서 간 관계

```
creation.md
    └─→ execution.md (생성 후 실행)

execution.md
    ├─→ ../protocols/ (프로토콜 감지로 제어)
    └─→ termination.md (종료)

termination.md
    └─→ ../checkpoint/ (종료 전 상태 저장)
```

## 🔗 관련 문서

- **프로토콜**: `../protocols/` - 프로토콜 처리로 상태 변경
- **모니터링**: `../monitoring/` - 상태 추적
- **Checkpoint**: `../checkpoint/` - 상태 저장/복구
- **웹 서버**: `../../../claude-code-server/docs/features/process-management.md`
