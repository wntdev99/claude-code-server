# 📚 문서 가이드

## 개요

이 프로젝트의 모든 문서는 **필요할 때 필요한 정보만 쉽게 찾을 수 있도록** 세분화되어 있습니다.

## 🎯 문서 철학

1. **세분화**: 큰 문서 하나보다 작고 집중된 여러 문서
2. **선택적 참조**: 모든 문서를 읽을 필요 없이 필요한 것만
3. **계층적 구조**: README → 상세 문서 → 구현 예시
4. **명확한 위치**: 각 주제별로 전용 폴더

## 📂 전체 문서 구조

```
claude-code-server/
│
├── README.md                    # 프로젝트 전체 개요
├── DOCUMENTATION_GUIDE.md       # 이 문서 (문서 네비게이션 가이드)
│
├── docs/                        # 프로젝트 레벨 문서
│   ├── ARCHITECTURE.md          # 시스템 아키텍처
│   ├── API.md                   # API 명세
│   ├── WORKFLOWS.md             # 워크플로우
│   ├── DEVELOPMENT.md           # 개발 가이드
│   └── FEATURES.md              # 기능 명세 (기존)
│
├── guide/                       # 에이전트 가이드 (24개, 기존)
│   ├── planning/                # 기획 가이드 (9개)
│   ├── design/                  # 설계 가이드 (5개)
│   ├── development/             # 개발 가이드 (6개)
│   ├── review/                  # 리뷰 가이드 (1개)
│   └── verification/            # 검증 가이드 (3개)
│
└── packages/                    # 각 패키지별 문서
    │
    ├── claude-code-server/      # 웹 서버
    │   ├── CLAUDE.md            # 전체 개요 (간결)
    │   ├── README.md            # 패키지 설명
    │   └── docs/                # 상세 문서
    │       ├── README.md        # docs 폴더 가이드
    │       ├── architecture/    # 아키텍처
    │       ├── development/     # 개발 환경
    │       ├── features/        # 기능 구현
    │       ├── security/        # 보안
    │       └── api/             # API 설계
    │
    ├── agent-manager/           # 에이전트 관리자
    │   ├── CLAUDE.md            # 전체 개요 (간결)
    │   ├── README.md            # 패키지 설명
    │   └── docs/                # 상세 문서
    │       ├── README.md        # docs 폴더 가이드
    │       ├── lifecycle/       # 생명주기 관리
    │       ├── protocols/       # 프로토콜 처리
    │       ├── queue/           # 대기열 관리
    │       ├── checkpoint/      # Checkpoint 시스템
    │       └── monitoring/      # 모니터링
    │
    └── sub-agent/               # 서브 에이전트
        ├── CLAUDE.md            # 전체 개요 (간결)
        ├── README.md            # 패키지 설명
        └── docs/                # 상세 문서
            ├── README.md        # docs 폴더 가이드
            ├── workflows/       # 작업 타입별 워크플로우
            ├── protocols/       # 통신 프로토콜
            ├── deliverables/    # 산출물 생성 규칙
            └── verification/    # 검증 기준
```

## 🔍 문서 찾기

### "프로젝트가 뭔지 알고 싶어요"
→ `/README.md`

### "시스템 아키텍처를 이해하고 싶어요"
→ `/docs/ARCHITECTURE.md`

### "웹 서버를 개발하고 싶어요"
```
1. packages/claude-code-server/CLAUDE.md (개요)
2. packages/claude-code-server/docs/README.md (문서 구조)
3. 필요한 주제의 문서 (예: docs/features/sse-streaming.md)
```

### "에이전트 관리자를 개발하고 싶어요"
```
1. packages/agent-manager/CLAUDE.md (개요)
2. packages/agent-manager/docs/README.md (문서 구조)
3. 필요한 주제의 문서 (예: docs/protocols/dependency.md)
```

### "서브 에이전트로 작업하고 싶어요"
```
1. packages/sub-agent/CLAUDE.md (개요)
2. packages/sub-agent/docs/README.md (문서 구조)
3. 작업 타입 확인 (docs/workflows/[타입].md)
4. /guide/ 폴더의 상세 가이드 참조
```

## 📖 CLAUDE.md 파일의 역할

각 패키지의 `CLAUDE.md` 파일은 **전체 개요와 네비게이션 가이드** 역할을 합니다:

### ✅ CLAUDE.md에 포함된 것
- 패키지의 책임과 역할
- 문서 구조 설명
- 빠른 시작 가이드
- 일반적인 작업 흐름
- 전체 문서 목록

### ❌ CLAUDE.md에 포함되지 않은 것
- 상세한 구현 방법 → `docs/` 폴더 참조
- 코드 예시 → 각 주제별 문서 참조
- 전체 API 명세 → `docs/api/` 참조
- 보안 상세 구현 → `docs/security/` 참조

## 🎓 학습 경로

### 1단계: 전체 이해
```
/README.md
→ 프로젝트 개요, 3-tier 아키텍처 이해

/docs/ARCHITECTURE.md
→ 시스템 구조 상세 이해
```

### 2단계: 패키지 선택
```
웹 서버 개발자
→ packages/claude-code-server/CLAUDE.md

에이전트 관리자 개발자
→ packages/agent-manager/CLAUDE.md

서브 에이전트 (작업 수행)
→ packages/sub-agent/CLAUDE.md
```

### 3단계: 상세 문서
```
각 패키지의 docs/README.md를 읽고
→ 필요한 주제의 문서를 선택적으로 읽기
```

## 💡 효율적인 문서 활용법

### ✅ 권장 방법

**1. README 먼저**
```
각 폴더에 들어갈 때마다 README.md를 먼저 읽으세요.
폴더 구조와 문서 목록을 파악할 수 있습니다.
```

**2. 목적에 맞는 문서만**
```
SSE 구현이 필요하면
→ packages/claude-code-server/docs/features/sse-streaming.md만 읽기

모든 features 문서를 읽을 필요 없음!
```

**3. 관련 문서 링크 활용**
```
각 문서 끝의 "관련 문서" 섹션을 따라가면
연관된 정보를 쉽게 찾을 수 있습니다.
```

### ❌ 비효율적인 방법

**1. 모든 문서 순서대로 읽기**
```
❌ 시간 낭비
✅ 필요한 것만 선택적으로 읽기
```

**2. 문서 없이 코딩 시작**
```
❌ 잘못된 구조, 중복 작업
✅ 관련 문서를 먼저 확인
```

**3. 오래된 정보 참조**
```
❌ 큰 CLAUDE.md 파일 (이제 개요만 포함)
✅ docs/ 폴더의 최신 상세 문서
```

## 🔗 문서 간 관계

### claude-code-server (웹 서버)
```
architecture/nextjs-structure.md
    ├─→ features/sse-streaming.md (SSE 기능)
    ├─→ features/process-management.md (프로세스 관리)
    └─→ security/ (보안)

features/process-management.md
    └─→ ../../agent-manager/docs/lifecycle/ (에이전트 생명주기)
```

### agent-manager (에이전트 관리자)
```
lifecycle/creation.md
    └─→ protocols/ (생성 후 프로토콜 처리)

protocols/dependency.md
    ├─→ lifecycle/execution.md (일시중지/재개)
    └─→ checkpoint/creation.md (상태 저장)
```

### sub-agent (서브 에이전트)
```
workflows/create-app.md
    ├─→ protocols/dependency-request.md (의존성 요청)
    ├─→ deliverables/documents.md (문서 생성)
    ├─→ verification/phase1-planning.md (검증)
    └─→ /guide/planning/*.md (상세 가이드)
```

## 📊 문서 타입별 위치

### 아키텍처 & 설계
- **전체 시스템**: `/docs/ARCHITECTURE.md`
- **웹 서버 구조**: `packages/claude-code-server/docs/architecture/`
- **에이전트 관리**: `packages/agent-manager/docs/lifecycle/`

### 기능 구현
- **웹 서버 기능**: `packages/claude-code-server/docs/features/`
- **프로토콜 처리**: `packages/agent-manager/docs/protocols/`
- **작업 수행**: `packages/sub-agent/docs/workflows/`

### API 명세
- **전체 API**: `/docs/API.md`
- **API 설계**: `packages/claude-code-server/docs/api/`

### 보안
- **웹 서버 보안**: `packages/claude-code-server/docs/security/`

### 개발 환경
- **전체 개발**: `/docs/DEVELOPMENT.md`
- **웹 서버 개발**: `packages/claude-code-server/docs/development/`

### 가이드 (에이전트용)
- **모든 가이드**: `/guide/`
  - 기획: `planning/`
  - 설계: `design/`
  - 개발: `development/`
  - 리뷰: `review/`
  - 검증: `verification/`

## 🆘 자주 묻는 질문

### "어떤 문서부터 읽어야 하나요?"
역할에 따라 다릅니다:
- **프로젝트 매니저**: `/README.md`, `/docs/FEATURES.md`
- **웹 개발자**: `packages/claude-code-server/CLAUDE.md`
- **백엔드 개발자**: `packages/agent-manager/CLAUDE.md`
- **AI 에이전트**: `packages/sub-agent/CLAUDE.md`

### "문서가 너무 많아요"
모든 문서를 읽을 필요 없습니다!
- README.md로 구조 파악
- 필요한 주제만 선택적으로 읽기
- 나머지는 필요할 때 참조

### "최신 정보는 어디에?"
- **각 패키지의 `docs/` 폴더**가 최신 상세 정보
- **CLAUDE.md**는 개요와 네비게이션만

### "코드 예시는 어디에?"
- **주제별 문서**에 포함 (예: `features/sse-streaming.md`)
- **아키텍처 문서**에 패턴 설명

## 🎯 시나리오별 가이드

### 시나리오 1: "SSE 로그 스트리밍 구현하기"
```
1. packages/claude-code-server/CLAUDE.md
   → "로그 스트리밍" 항목 확인

2. packages/claude-code-server/docs/features/sse-streaming.md
   → 상세 구현 방법

3. packages/claude-code-server/docs/architecture/api-routes.md
   → API Route 구조 이해 (필요시)

4. packages/claude-code-server/docs/security/rate-limiting.md
   → 보안 적용 (필요시)
```

### 시나리오 2: "Phase 완료 신호 감지하기"
```
1. packages/agent-manager/CLAUDE.md
   → "Phase 완료 처리" 흐름 확인

2. packages/agent-manager/docs/protocols/phase-completion.md
   → 상세 프로토콜 파싱 방법

3. packages/agent-manager/docs/lifecycle/execution.md
   → 에이전트 일시 중지 방법
```

### 시나리오 3: "create_app 작업 수행하기"
```
1. packages/sub-agent/CLAUDE.md
   → 작업 타입 확인

2. packages/sub-agent/docs/workflows/create-app.md
   → 워크플로우 이해

3. /guide/planning/01_idea.md
   → Phase 1 Step 1 가이드

4. packages/sub-agent/docs/deliverables/documents.md
   → 문서 작성 규칙

5. packages/sub-agent/docs/verification/phase1-planning.md
   → 검증 기준 확인
```

## 📝 문서 업데이트 원칙

1. **CLAUDE.md**: 전체 개요만 유지, 상세 내용은 docs/로
2. **README.md**: 각 폴더의 목적과 문서 목록
3. **주제별 문서**: 한 가지 주제에 집중
4. **관련 문서 링크**: 항상 최신으로 유지

## 🔄 다음 단계

1. **역할 확인** - 웹 서버? 에이전트 관리자? 서브 에이전트?
2. **해당 CLAUDE.md 읽기** - 전체 개요 파악
3. **docs/README.md 읽기** - 문서 구조 이해
4. **필요한 문서만 선택적으로 읽기** - 효율적으로!

---

**핵심 원칙**: 모든 문서를 읽지 마세요. 필요한 것만 선택하세요!

**문서 위치**: 이 가이드는 `/DOCUMENTATION_GUIDE.md`에 있습니다.
